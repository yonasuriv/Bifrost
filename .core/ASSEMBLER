#!/bin/sh

origin="." # Used for debugging only
#origin=~/.local/share/Bifrost # Bifrost Path
#. $origin/.core/ASSEMBLER # Assemble 

# Core
#. $origin/.core/CONTROL
. $origin/.core/DEPENDENCIES
. $origin/.core/TARGET
. $origin/.core/PROJECTS
. $origin/.core/ARGUMENTS
. $origin/.core/UPDATER

# Assets
. $origin/.assets/CSS
. $origin/.assets/LOGO

# Menu
. $origin/.menu/MAIN
. $origin/.menu/NETSCAN
. $origin/.menu/FIND-DCIP
. $origin/.menu/ZONE-TRANSFER
. $origin/.menu/LGA-SMB-SHARE
. $origin/.menu/FIND-USER-LIST
. $origin/.menu/POISONING
. $origin/.menu/COERCE
. $origin/.menu/LDAP-ENUM

# Info

. $origin/.info/CREDITS
. $origin/.info/LEGEND
. $origin/.info/MAN
. $origin/.core/CONTROL

start () {
    #sudoreq
    clear
    logo
    print_version
    target_data
    menu
    menu_select
}
